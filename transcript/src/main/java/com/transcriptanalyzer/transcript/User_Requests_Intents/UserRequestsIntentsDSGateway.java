package com.transcriptanalyzer.transcript.User_Requests_Intents;

public interface UserRequestsIntentsDSGateway {

    //Contain any functions that we need to make the repository run
    //In otherwords what are steps we need to take to ensure we can return the best intents to the user
    // This would include the save function so that when a transcript is inserted into the database the class dictionar
    //would increase by one
}
